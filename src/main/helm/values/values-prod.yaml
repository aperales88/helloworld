namespace: prod

ingress:
  hosts:
    - host: helloworld.local
      paths:
        - path: /
          pathType: ImplementationSpecific
  tls:
    - secretName: secret-tls
      hosts:
        - helloword.local